function vanilla(redirectTo = undefined, htmlTpl = undefined, message = undefined) {
    // TODO: implement
    // we should either:
    // 1/ redirect to rdirectTo
    // 2/ present htmlTpl passed to us as string
    // 3/ show a white page w/ message
    // 4/ fallback to white page
}

module.exports = vanilla;
